<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Task History</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <style>
      body {
        background-color: #f9f9f9;
        font-family: Arial, sans-serif;
        padding: 2rem;
      }
      .container {
        background: white;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .task-item {
        display: flex;
        justify-content: space-between;
        padding: 0.75rem;
        border-bottom: 1px solid #eee;
      }
      .task-item:last-child {
        border-bottom: none;
      }
      .timestamp {
        font-size: 0.85rem;
        color: gray;
      }
      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>📜 Task Completion History</h2>
        <form
          action="/clear-history"
          method="POST"
          onsubmit="return confirm('Are you sure you want to delete all history?');"
        >
          <button type="submit" class="btn btn-danger">🗑️ Clear History</button>
        </form>
      </div>

      <% if (completedTasks.length === 0) { %>
      <p class="text-muted">No completed tasks found.</p>
      <% } else { %> <% completedTasks.forEach(task => { %>
      <div class="task-item">
        <div>
          <strong><%= task.title %></strong><br />
          <span class="timestamp">Completed at: <%= task.completedAt %></span>
        </div>
      </div>
      <% }) %> <% } %>

      <div class="mt-4">
        <a href="/" class="btn btn-secondary">⬅️ Back to Tasks</a>
      </div>
    </div>
  </body>
</html>
